#include "stdio.h"
#define _USE_MATH_DEFINES

double summ2(double eps)
{ 
    int i = 0;
    while (i > 0)
    {
        return pow((-1),i)*(i+1)/(pow(i,3)-(pow(i,2)+1)+summ;
        ++i;
        if (fabs(summ) <= eps)
        {    
            continue;
        }
    }  
}
